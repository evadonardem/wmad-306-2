name: hero_battle
description: "A simple hero battle game."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  dio: ^5.9.2
  # cached_network_image: ^3.4.1  # Temporarily removed for memory optimization
  # sqflite: ^2.4.2  # Temporarily removed for memory optimization
  # shared_preferences: ^2.5.5  # Temporarily removed for Android embedding issues
  path: ^1.9.1
  # flutter_animate: ^4.5.2  # Temporarily removed for memory optimization

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

# 05 My First Flutter App - Evaluation Results

Total Points: 25

| Student | Score | Deduction % | Evaluator Notes |
| :--- | :--- | :--- | :--- |
| Abinon | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Acosta | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Andaya | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Bala-oy | 24 | 60% | Increment, Decrement, Reset. Strong UI safety (check inside `_decrementCounter`). |
| Balansi | 13 | 60% | No counter functionality implemented (implemented Pokédex instead). |
| Bangonan | 24 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Calderon | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Catayao | 23 | - | Increment, Decrement, Reset. UI safety check implemented with null-safe integer. |
| Ceasar | 23 | 60% | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Cirilo | 18 | - | Increment and Decrement only. No Reset. No UI safety check (can go negative). |
| Cuyangan | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Damugo | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Godoy | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Ho | 23 | 60% | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Hunas | 25 | 40% | Increment, Decrement, Reset. High polish: AnimatedContainer, dynamic colors, and status messages. |
| Icad | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Lang-odan | 23 | 40% | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Laron | 23 | 60% | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Rosalejos | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
| Sarino | 22 | - | Increment, Decrement, Reset. No UI safety check (can go negative). |
| Visaya | 23 | - | Increment, Decrement, Reset. UI safety check (`_counter > 0`) implemented. |
